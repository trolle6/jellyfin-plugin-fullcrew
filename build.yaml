---
name: "Full Crew"
guid: "a8f3c2e1-9b4d-4f6a-8e2c-1d5b7a9c0e3f"
version: "1.6.1.0"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Full cast and crew by department, break bumpers, library stats, studio pages, and optional scene identify"
description: >
  Fetches complete TMDB cast and crew for movies and series, groups people by
  department, and shows them in a polished accordion on Jellyfin Web item detail
  pages. Includes a nostalgia Break Bumper action, a Library Stats page
  (genres, people, collections, resolution/HDR/codecs, and more), studio
  detail pages with TMDB company metadata plus library titles, and optional
  opt-in scene identify (OpenAI Vision) during web playback.
category: "Metadata"
owner: "trolle6"
artifacts:
- "Jellyfin.Plugin.FullCrew.dll"
changelog: >
  Scene identify 1.6.1: hotkey is Y (not I/K) so it does not clash with Jellyfin
  settings. Still opt-in, one frame per ask, cast-grounded OpenAI Vision.
